// tests/unit/authRoutes.test.js

describe("register function", () => {
    it("should register a new user with valid data", () => {
        // Votre test ici
    });

    it("should handle missing data gracefully", () => {
        // Votre test ici
    });

    it("should handle duplicate email address", () => {
        // Votre test ici
    });
});
